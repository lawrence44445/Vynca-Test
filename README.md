## 🛠️ Setup Instructions

### 📦 Install dependencies

#### In the `backend/` directory:

```bash
cd backend
npm install
node server.js

#### In the `my-app/` directory:
cd ../my-app
npm install
npm start

